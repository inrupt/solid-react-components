import React from 'react';
import { storiesOf } from '@storybook/react';
import {
    PrivateRoute
} from "@solid-react-components";

storiesOf('Private Route', module).add('default', () => <h1> Private Route</h1>);
